# Copy to `.env.local` or `.env` for local dev

# Supabase project URL (Project Settings -> API)
VITE_SUPABASE_URL=

# Supabase anon public key (Project Settings -> API)
VITE_SUPABASE_ANON_KEY=

# Optional: App URL used in OAuth redirects during local dev
VITE_APP_URL=http://localhost:8080

# Optional: service role key for local DB inspection scripts ONLY.
# Never expose this in client code or commit it.
# SUPABASE_SERVICE_ROLE_KEY=

# Assistant API (local server base URL for the frontend)
VITE_ASSISTANT_BASE_URL=http://localhost:8787

# OpenAI (server-side only; do not expose to client)
# OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.5

# Stripe (server-side only; for assistant-server)
# STRIPE_SECRET_KEY=
# STRIPE_PRICE_PRO=
# STRIPE_PRICE_ID=
# STRIPE_PRODUCT_ID=
# STRIPE_SUCCESS_URL=http://localhost:8080/billing/success
# STRIPE_CANCEL_URL=http://localhost:8080/billing/cancel
# STRIPE_WEBHOOK_SECRET=
# STRIPE_DEFAULT_CUSTOMER_ID=

# Optional: force plan in status endpoint (dev/testing)
# FORCE_PLAN=pro

# Frontend can optionally provide a default customer id for portal
# VITE_STRIPE_CUSTOMER_ID=
